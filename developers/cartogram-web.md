# cartogram-docker and cartogram-web

We strongly recommend using [VSCode](https://code.visualstudio.com/) for development.

## Setting Up Development Environment

The development setup enables hot-reloading, allowing you to see code changes reflected in the website in real-time.

1. Follow instrunctions in [Local Docker Deployment](/tutorials/local).

2. Clone `cartogram-web` inside `cartogram-docker`:

   ```shell script
   cd cartogram-docker
   git clone https://github.com/go-cart-io/cartogram-web.git
   ```

3. Install the [Dev Containers extension](https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.remote-containers). If you plan to work with other remote extensions in VS Code, you may choose to install the [Remote Development extension pack](https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.vscode-remote-extensionpack).

4. Lunch Visual Studio Code and open `cartogram-docker` folder.

5. Press F1 to open the command palette. Type "Reopen in Container" and select it.

6. Wait for the container to build. Once built, check the bottom left corner - it should show the Dev Container name.

7. Install recommended extensions when prompted.

8. Open terminal and run:

   ```shell script
   npm run dev:js
   ```

   Please note that the URL generated by this command will not display anything. Simply ignore open in browser prompt and proceed to the next step.

9. Next, open **another** terminal and run:

   ```shell script
   npm run dev:py
   ```

You can now access the locally running go-cart.io website at [http://localhost:5000](http://localhost:5000). When you make changes to the code in devcontainer, their respective servers will reload automatically when you save your changes.

In case you want to make changes to the code in the `cartogram-docker` repository, you have to do it outside devcontainer. Once done, press F1 to open the command palette and select "Rebuild and Reopen in Container".

You may want to checkout dev branch for the latest (but error-prone) code.

## Debugging

For debugging in VSCode, refer to the official documentation: [https://code.visualstudio.com/docs/debugtest/debugging](https://code.visualstudio.com/docs/debugtest/debugging).

The cartogram-web project includes preconfigured debugging setups for:

- **Python**: Python Debugger: Flask
- **JavaScript**: Launch Chrome against localhost

**Important Notes** for Python Debugging:

- Stop the running dev server by pressing Ctrl+C in the terminal where `npm run dev:py` is active before starting the debugger.
- The VSCode debugger disables hot-reload to avoid conflicts.

## Testing

### Tablet testing via local network

If you want to access the local instance via tablets, please follow the instructions below.

#### Windows

First, you must allow other devices to access docker. If you use wsl on Windows, you can run the following command in command prompt (may need to run as administrator):

```shell script
netsh advfirewall firewall add rule name="Allowing LAN connections" dir=in action=allow protocol=TCP localport=5000

bash.exe -c "ip -4 addr show eth0 | grep -oP '(?<=inet\s)\d+(\.\d+){3}'"

netsh interface portproxy add v4tov4 listenaddress=0.0.0.0 listenport=5000 connectaddress=<your-docker-ip> connectport=5000
```

Reference: https://stackoverflow.com/questions/61002681/connecting-to-wsl2-server-via-local-network

If you are running the application in development mode, you should allow other computher to access port 5173 (for Windows, you can see https://www.devopinion.com/access-localhost-from-another-computer-on-the-same-network/). If you cannot access 5173, check firewall, e.g., delete rules that block node.js.

You should be able to access the application using `http:\\your-local-ip:5000`

#### macOS (and iPad)

Work in progress. Contact @adisidev for help getting set up.

### Tablet testing via USB (Andriod)

Please follow this tutorial https://developer.chrome.com/docs/devtools/remote-debugging/. Then, setup port forwarding for port 5000 and 5173, see https://developer.chrome.com/docs/devtools/remote-debugging/local-server/.

## Contributing

We welcome and encourage contributions from the community! To contribute:

- **Submit a Pull Request (PR)** – Open a PR to propose your changes for review.
- **Follow Commit Guidelines** – All commit messages must adhere to [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/).

**Core Developers**: For deployment workflows, refer to the [Deployment Instructions](/developers/deploy) for pushing changes to go-cart.io.

### Coding standard and quality

To maintain coding standards and quality, we use Prettier and ESLint for the frontend, and Ruff for the backend. With this setup, the IDE should handle formatting and most linting on save. Some linting issues may require manual fixes, but the IDE will automatically inform you as you code.

Otherwise, you should manually run the following command:

For frontend:

```shell script
cd cartogram-web/frontend
npm run format
npm run lint
```

For backend:

```shell script
ruff format
ruff check --fix
```

Please fix all linting issues before commit.
